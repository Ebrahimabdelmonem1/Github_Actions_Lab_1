on:
  workflow_dispatch:
jobs:
  example:
    runs-on: ubuntu-latest
    steps:
    - name: set API Key
      run: echo "API_KEY=123" >> $GITHUB_ENV
    - name: use API Key
      run: |
        echo "::add-mask::$API_KEY"
        echo "using API_Key = $API_KEY"
    
    
    
    # - name: check if a file exist
    #   run: |
    #     if [ -f "config.yaml" ]; then
    #       echo "::debug::config.yaml exists"
    #     else
    #       echo "::error::config.yaml not exist"
    #     fi  
    
    
    
    
    # - name: set API key
    #   run: echo "API_KEY=123" >> $GITHUB_ENV
    # - name: use API Key
    #   run: echo "API Key = $API_KEY" 
      
      # - run: |
      #     deprecated=true  # this acts as variable with name deprecated
      #     if [[ deprecated ]]; then
      #       echo "::warning::deprecated function found"
      #     fi  
