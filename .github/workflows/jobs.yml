on: 
  workflow_dispatch:

jobs:
  job_1:
    runs-on: ubuntu-latest
    steps:
      - run: |
          npm install
          npm run build
        working-directory: ./src
        shell: bash  
       
      - run: print %ENV
        shell: perl {0}
  # job_2: 
  #   runs-on: ubuntu-latest
  #   needs: job_1
  #   steps:
  #     - run: echo "job ${{ github.job }}"
  # job_3:
  #   runs-on: ubuntu-latest
  #   needs: job_1
  #   steps:
  #     - run: echo "job ${{ github.job }}"
  # job_4:
  #   runs-on: ubuntu-latest
    # needs: [job_3,job_2]
    # steps:
    #   - run: echo "Job ${{ github.job }} "
